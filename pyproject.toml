[project]
name = "time-series-forecasting-solar-irradiance-with-arim"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.10.0",]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
